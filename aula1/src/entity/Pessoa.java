package entity;

// classe com acesso Default
// ou seja é acessível apenas dentro do próprio pacote
class Pessoa {

    // default -é acessível apenas dentro do próprio pacote
    String nome;
    int idade;
    public String endereco;
}
